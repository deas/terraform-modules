###############################################################################################
###     Managed via [Modulesync](https://github.com/MediaMarktSaturn/cp-modulesync-config)  ###
###             Do not make any changes manually as they will be overridden                 ###
###############################################################################################

# Local .terraform directories
.terraform/
# .tfstate files
*.tfstate
*.tfstate.*
# log files
*.log
# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# .tfvars files except for those used for validation checks
# *.tfvars
# Exclude test output from validation checks, keep sample .tfvars files
!validation/**/*.tfvars
validation/*.tfplan
validation/*.txt
# credential files
*credentials.json
*credentials*.json
# Kitchen directories
terraform.tfstate.d/*
.kitchen/
# vscode
.vscode/
# IntelliJ
*.iml
/.idea

# local development folders
secrets/

# Ignore lockfile from TF 0.14+
.terraform.lock.hcl

terraform.tfvars
